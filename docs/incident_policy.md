# Incident Policy

Incidents are first-class proving artifacts, not log noise.

Incident categories include safety guard failures, data-quality failures, leakage failures, drift failures, reconciliation failures, staleness failures, runbook failures, strategy instability, live-danger evidence, and unknown errors.

Critical or high incidents block proving readiness. Live-danger evidence must fail closed and can never be treated as permission to trade.
