from __future__ import annotations


def walk_forward_placeholder() -> dict[str, str]:
    return {
        "status": "future_work",
        "note": "Walk-forward validation is documented for later phases.",
    }
